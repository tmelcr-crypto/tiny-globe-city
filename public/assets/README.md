Placeholder folder for models, textures and sounds.

Nothing lives here yet — every model in the game is built from code in
`src/entities/`. The look those placeholders are aiming at is in
`docs/art/` (style reference sheet plus notes on scale, origins and
footprints that real assets have to match).
